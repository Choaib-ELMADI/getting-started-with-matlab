1. Convert your command window code into a live script
    > Open Command History
    > Select the commands
    > Right click
    > Create Live Script

2. Create beautiful docs with this:
    * Split code: %% sectionName
    * Add text
    * ...
    
    * $M_{some_text}$
    * $\sin$, $\cos$
    * $\times$
    * $\alpha$
    * $\beta$
    * $\delta$
    * $\gamma$
    * ...

3. Import data tools (csv, xlsx, ...)

4. Grabit Add-Ons:
    ==> Extract data from an image

    > Install grabit add-ons
    > Run grabit.m file
    > Select the image to extract data from
    > Calibrate the x and the y axes (Min and Max values)
    > Start positioning the points
    > Save extracted data

5. Run and Time:
    ==> This will open the "Profiler for Improving Performance" window
    ==> Show details of running a function

6. Parallel Pool: parpool...